{
  "name": "wiamapp-marketing-deploy",
  "version": "1.0.0",
  "private": true,
  "description": "Render entrypoint when WiamAppMarketing Root Directory is the monorepo root. Blueprint services should use rootDir: marketing instead.",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "npm install --prefix marketing && npm run build --prefix marketing",
    "start": "npm run start --prefix marketing"
  }
}
